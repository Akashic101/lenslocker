/** Pass to `depends()` in home `load` and `invalidate()` after uploads so the grid refetches (SvelteKit client cache). */
export const transformed_media_depends_key = 'lenslocker:transformed_media';
