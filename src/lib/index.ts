/**
 * Shared client + isomorphic modules under `$lib`.
 *
 * - `cache/` — SvelteKit `depends()` / `invalidate()` keys
 * - `config/` — defaults and format/limit constants
 * - `gallery/` — catalog, editable fields, extensions, transformed URLs
 * - `server/` — server-only code (DB, services, etc.)
 * - `paraglide/` — generated i18n (Paraglide)
 * - `assets/` — static assets imported from the client
 */
