/** Invalidate after changing dashboard “needs attention” criteria so the gallery load refetches. */
export const dashboard_attention_settings_depends_key = 'lenslocker:dashboard_attention_settings';
